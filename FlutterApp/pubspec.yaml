name: aqua_link_grow_smart
description: AquaLink Smart Irrigation System - IFS325/353 Project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI Components
  cupertino_icons: ^1.0.2
  
  # State Management
  provider: ^6.1.1
  
  # Navigation
  go_router: ^13.0.0
  
  # Charts
  fl_chart: ^0.66.0
  
  # Icons
  lucide_icons_flutter: ^1.0.0
  
  # Toast/Snackbar
  fluttertoast: ^8.2.4
  
  # Utilities
  intl: ^0.19.0
  
  # Storage
  shared_preferences: ^2.2.2
  
  # HTTP requests
  http: ^1.1.0
  
  # WebSocket (cross-platform)
  web_socket_channel: ^2.4.0
  
  # MQTT Client (direct MQTT connection)
  mqtt_client: ^10.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/
    - public/

